[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "grain-guard"
version = "0.1.0"
description = "GrainGuard domain simulation for TattleTots — precision pest and weed management with physical drone/tractor Tots"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [{ name = "Benjamin Kirkup", email = "bckirkup@gmail.com" }]
keywords = [
    "simulation",
    "agriculture",
    "pest-management",
    "precision-ag",
    "agent-based",
    "tattletots",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24",
    "pydantic>=2.0",
    "domain-runner @ git+https://github.com/bckirkup/domain-runner.git",
    "tattletots @ git+https://github.com/bckirkup/TattleTots.git",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
    "mypy>=1.8",
    "pre-commit>=3.0",
]
gpu = [
    "cupy-cuda12x>=13.0",
]

[project.scripts]
grain-guard = "grain_guard.cli:main"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/grain_guard"]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501", "SIM108"]

[tool.ruff.lint.isort]
known-first-party = ["grain_guard"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
packages = ["grain_guard"]
mypy_path = ["src"]

[[tool.mypy.overrides]]
module = "domain_runner.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers -ra"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "smoke: smoke test scenario validating end-to-end grain guard simulation",
    "integration: integration tests requiring TattleTots engine",
]

[tool.coverage.run]
source = ["grain_guard"]

[tool.coverage.report]
show_missing = true
skip_empty = true
